---
'did-jwt-vc': major
---

BREAKING CHANGE: the package exports map has changed to new paths. The functionality of the library is unchanged.

Along with this change, the package now uses plain `tsc` to compile instead of microbundle, and now we are targeting ES2024.
